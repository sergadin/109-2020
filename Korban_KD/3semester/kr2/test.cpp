#include <iostream>
#include "vector_clock.h"

using namespace std

int main()
{
    vector_clock A(2, 0), b(2, 1);
    b.local_add(2)
    A,local_add();
    cin << A << endl;
    cin << A << endl;
    try
    {
        A.receve(b);
        cin << A << endl;
        cin << A < B << endl;
        
    }
    return 0;
}

#include"stdio.h"
#include"stdlib.h"
#include"math.h"
#include"clin.h"

double clin(double **matrix2d, int n_rows, int n_cols)
{
	int k, j, i, n = n_rows;
	double d, s;
	double *x, *b;
	for(int i = 0; i < n_rows; i ++)
	{
			b[i] = matrix2d[i][n_cols-1];
	}

	for(i = 0; i < n_cols; i ++)
	{
		printf("%lf", x[i]);
	}
	return 0;
}


